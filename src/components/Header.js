import React from 'react';

const Header = (props) => {
    return (
        <header>
            <div>
                {props.home} 
                <span>ll</span>
            </div>
            <button>YL</button>
        </header>
    )
}

export default Header;
